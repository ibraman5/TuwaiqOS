# Tuwaiq Applications

Product applications should prefer **Tuwaiq Platform APIs** over direct
Linux-only interfaces when a Platform surface exists.

D0 ships the upstream desktop apps needed for smoke (terminal, file manager)
via the Linux/KDE stack. Custom Tuwaiq apps come in later milestones.
