# TuwaiqOS Product D1 — Desktop Identity + Connectivity
#
# Branch: product/d1-desktop-identity-connectivity
# Baseline: accepted D0 at 9e4e385 (product/desktop-foundation)
#
# Pillars:
# A) Tuwaiq desktop identity (themes, wallpaper, Plasma/SDDM/Konsole branding)
# B) Connectivity foundation (NetworkManager + plasma-nm + status JSON)
#
# Apply onto preserved D0 disk (Docker volume tuwaiqos-d0-work):
#   docker run --privileged -v tuwaiqos-d0-work:/work -v ${PWD}:/src -w /src ...
#   bash product/build/apply-d1.sh
#
# Boot (Windows host, D0 display/disk preserved + virtio-net):
#   pwsh scripts/boot-product-gui.ps1 -Seconds 240
